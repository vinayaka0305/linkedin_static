import React from "react";

const ViewSettings = () => {
  return (
    <div
      style={{
        backgroundColor: "#1b1f23",
        height: "fit-content",
        padding: "1rem 3rem 1rem 1rem",
        borderRadius: "0.5rem",
        fontWeight: "600",
      }}
    >
      <p style={{ marginBottom: "0.5rem" }}>Manage your Notifications</p>
      <a href="#" style={{ color: "rgb(30, 184, 255)" }}>
        View Settings
      </a>
    </div>
  );
};

export default ViewSettings;
